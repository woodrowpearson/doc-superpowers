<!-- Generated by doc-superpowers | 2026-05-16 | branch: feat/granular-install -->

# Documentation Audit Report — 2026-05-16

**Branch:** `feat/granular-install` (5 commits ahead of `main`)
**Trigger:** post-implementation audit + update after `feat/granular-install` work (v2.12.0).
**Freshness:** `doc-tools.sh check-freshness` → 17 current, 10 stale (all `code_changed`), 0 missing, 0 deprecated, 0 untracked.

## Code Changes Driving Staleness

| Commit | Scope |
|--------|-------|
| `10ed677` feat(doc-tools): add `tools install\|uninstall\|status` subcommand | `scripts/doc-tools.sh` (+208 LoC `cmd_tools`) |
| `1da70e8` feat(hooks): add `state.sh` — install-state tracking module | `scripts/hooks/state.sh` (new, 224 LoC + concurrency-contract note) |
| `c2496da` feat(hooks): granular install --ci with `--workflows` + state-respect | `scripts/hooks/install.sh` (rewrote `install_ci`, `uninstall_ci`, `status_ci`; new flags `--workflows=`, `--helpers=`, `--force`, `--transient`) |
| `f063b04` test(hooks,doc-tools): +24 tests covering granular install + tools subcommand + state | `scripts/test-hooks.sh` (295→302), `scripts/test-doc-tools.sh` (100→134) |
| `abb3e64` docs: SKILL.md + README updates | `skills/doc-superpowers/SKILL.md` (+39), `README.md` (+15) |

Plus the in-flight review-fix commit (pending) addressing 8 review items: bash-3.2 empty-array guards, `_tools_extract_version` fallback, filesystem-derived `state_known_workflows`, caller assertion in `state_bootstrap`, cached jq in `status_ci`, single-writer contract note, test sourcing the canonical workflow list, new `uninstall --ci --workflows=none` test.

## Findings

### P1 Stale — Living docs out of sync with code

| Doc | Reason | Required Updates |
|-----|--------|------------------|
| `docs/architecture/system-overview.md` | `code_changed` (`SKILL.md`, `scripts/`, `references/` — 4 commits behind) | Tech Stack table: add `state.sh` (install-state tracking module). Bump `doc-tools.sh` subcommand count from 10 → 11 (adds `tools`). Hooks Engine container description: add granular `--workflows` flag, `--helpers`, `--force`, `--transient`, install-state file. New Key Decision: install-state tracking semantics. |
| `docs/codebase-guide.md` | `code_changed` (`SKILL.md`, `scripts/` — 4 commits behind) | Directory tree: add `scripts/hooks/state.sh`. Key Files table: add `state.sh` row. `doc-tools.sh` row: mention `tools` subcommand and bump count. `install.sh` row: mention granular flags + install-state file. Where to Find Things: add row for state file and granular install flags. |
| `docs/workflows/doc-superpowers.md` | `code_changed` (`SKILL.md`, `scripts/`, `references/` — 4 commits behind) | `hooks` Process section: add granular flags to subcommand example; add `--workflows=`, `--helpers=`, `--force`, `--transient` to a new "Granular CI install flags" subsection; document state file (`.claude/doc-superpowers/installed.json`) and the install-state contract; document the `tools install/uninstall/status` subcommand. Mermaid hooks workflow may need an annotation on state-respect. |
| `docs/guides/getting-started.md` | `code_changed` (`README.md`, `SKILL.md` — 1 commit behind) | Brief mention of `tools install/uninstall/status` and granular `--workflows=` flag in the hooks/installation section if such examples exist there. |

### P3 Stale — Status notes only (historical docs)

These docs describe prior work that has been substantially extended. The right fix is a one-line status note, not content rewrite:

| Doc | Why stale | Fix |
|-----|-----------|-----|
| `docs/superpowers/plans/2026-03-12-bundled-doc-tooling.md` | `doc-tools.sh` grew from 4 → 11 subcommands; plan implemented and extended | Append/amend status: "Implemented and substantially extended (now 11 subcommands including `tools` and `fragments`)." |
| `docs/superpowers/plans/2026-03-13-workflow-hooks-harness.md` | CI tier grew from 3 → 9 templates; install.sh got granular flags + state tracking | Append status: "Implemented and substantially extended in v2.12.0 (granular `--workflows` install with state tracking)." |
| `docs/superpowers/specs/2026-03-12-bundled-doc-tooling-design.md` | Same — implemented and extended | Amend status line. |
| `docs/superpowers/specs/2026-03-13-workflow-hooks-harness-design.md` | Same — implemented and extended | Amend status line. |
| `docs/plans/2026-05-12-pr-release-fragment-producer-and-consumer.md` | Marked completed in prior cycle; freshness re-flagged because newer commits touched referenced files | Append "Completed in v2.10.0" status if not already present, or refresh the index entry via `update-index`. |
| `docs/issues/2026-05-04-doc-index-metadata-rewrite-on-every-commit.md` | Issue remains open; only the index entry needs refresh | No content change; `update-index` only. |

### CLAUDE.md / README.md

- **CLAUDE.md** (worktree-level, gitignored elsewhere) is fine — repo root `CLAUDE.md` is documentation for this skill's own use and was synced in v2.10.0 doc-refresh; no granular-install-specific structural changes propagate to it.
- **README.md** was already touched by `abb3e64` to document the new flags; spot-check during update.

## Recommended Action

Apply P1 updates with focused edits (no full rewrite — preserve voice and existing sections), apply P3 status notes, then `update-index` to refresh hashes.
